/*
    Note: From this point I will use VS code
*/

#include "header.h"
#include <iostream>

using namespace std;

int main() {
    cout << "Let's use VS code instead" << endl;
    cout << "The global variable is " << global << endl;
    cout << "Please enter your foavorite number to replace the global number: ";
    cin >> global;
    cout << "Now the global variable is " << global << endl;
    return 0;
}
